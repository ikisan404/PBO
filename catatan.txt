import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Hero here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Hero extends Actor
{
    /**
     * Act - do whatever the Hero wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        if(Greenfoot.isKeyDown("d"))
        
            setLocation(getX()+3,getY());
        
        if(Greenfoot.isKeyDown("a"))
        
            setLocation(getX()-3,getY());
        
        
        if(Greenfoot.isKeyDown("w"))
        
            setLocation(getX(),getY()-3);
        
        if(Greenfoot.isKeyDown("s"))
        
            setLocation(getX(),getY()+3);
        
    }
}

=-====
import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Hero here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Hero extends Actor
{
    /**
     * Act - do whatever the Hero wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        // Add your action code here.
        move(25);
        int x = getX();
        int y = getY();
        if (getX()==799){
            setLocation(0,getY());
        }

    }
    {
        if(Greenfoot.isKeyDown(ẅ
        isKeyDown = "w"--> getX(0)+3;
    }
}
\

di java, static dan konstan itu berbeda
static itu tidak konstan

static bisa digunakna di method dan attribute, tanpa class
constractor explicit ketika kita ingin meminta ia melakukan sesuatu, jika tidak, lebih baik tidak isi explicit
====
TUGAS
POLYMORPHISM
OVERRIDING
OVERLOADING

KONSEP DASAR 
OOP
DATABASE KONSEPNYA JUGA
DATA STRUCTUR
JARINGAN

TUGASS
Bagaimana caranya agar si enemy ke ujung, mereka akan menghilang
keluar enemy secara random setiap, misal 5 detik


